An event-driven notification pipeline using Apache Kafka. A Flask producer API emits user and transaction events to Kafka topics, a Python consumer service reads those events and persists them to PostgreSQL, and a React dashboard shows live throughput. The full stack runs locally via Docker Compose with GitHub Actions CI. To implement a dead letter queue for failed messages and an idempotent consumer to handle Kafka's at-least-once delivery guarantee.